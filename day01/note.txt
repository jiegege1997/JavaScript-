JS
   JavaScript
	   LiveScript 网景  JavaScript
		 JScript  IE
组成
   ECMAScript  核心语法规则
	 DOM   对文档节点的操作   document   object  model
	 BOM   对浏览器的操作
注释
   单行注释 //
	 多行注释 /*     */
JS中
   保留字和关键字
	 关键字 有特殊功能的单词
	    var if else for while do switch break continue 
	 保留字 将来有可能有特殊功能
	    class interface static  super abstract...
JS变量
   var关键字声明一个变量 
	 var a = 1;
	 a = 2;
	 a = 'hello';
	 var a,b,c;
	 var a=1,b=2,c=3;
	 变量名命名规则
	   1.字母 数字 下划线 $组成
	   2.不能以数字、下划线开始
	   3.有特殊含义，采用小驼峰命名法
	    sayName
			sayHello
			username
	   4 不能使用关键字和保留字
数据类型
	 基本数据类型 5种
	    undefined 声明了但没有初始化的
			         显示将undefined赋值给一个变量的
			         就只有一个undefined
							 var a;
							 var b = undefined;
			null类型 

	 引用数据类型 3种
	 Object
	 array
	 function
